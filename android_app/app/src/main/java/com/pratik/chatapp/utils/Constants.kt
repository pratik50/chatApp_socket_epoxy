package com.pratik.chatapp.utils

object Constants {

    // Emulator localHost address (change as per server running port)
    const val SOCKET_URL = "http://10.0.2.2:3000/"

    //Do not change unless the server event name are updated.
    const val NEW_MESSAGE = "new_message"
    const val BROADCAST = "broadcast"

    const val USERNAME = "username"
    const val DATABASE_NAME = "chat_database"
}